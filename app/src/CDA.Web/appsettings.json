{
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft.AspNetCore": "Warning",
        "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
      }
    },
    "WriteTo": [
      { "Name": "Console" }
    ]
  },
  "AllowedHosts": "*",

  "ConnectionStrings": {
    "//": "Intentionally empty. This repository is public — a committed credential is a disclosed credential. Set the value in user secrets: dotnet user-secrets set \"ConnectionStrings:Cda\" \"...\" --project app/src/CDA.Web, or supply the environment variable ConnectionStrings__Cda. See Documentation/devplan.md section 2.1.",
    "Cda": ""
  }
}
